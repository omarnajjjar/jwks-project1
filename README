# JWKS Server Project

## Overview
This project implements a basic JSON Web Key Set (JWKS) server that provides public keys with unique identifiers (kid) for verifying JSON Web Tokens (JWTs). It includes an authentication endpoint and handles the issuance of JWTs with expired keys based on a query parameter. This server is built for educational purposes to demonstrate the handling of JWKS and JWTs in a RESTful API.

## Prerequisites
- Node.js (v14.x or higher recommended)
- npm (v6.x or higher)

## Installation

To set up the project environment, follow these steps:

1. Clone the repository:
2. Navigate to the project directory:
3. Install the necessary packages:

## Packages Used
This project utilizes the following npm packages:
- `express` for creating the server and handling HTTP requests.
- `jsonwebtoken` for generating and verifying JWTs.
- `morgan` for HTTP request logging.
- `uuid` for generating unique identifiers for the keys.
- `crypto` for RSA key pair generation and handling.


